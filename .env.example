# GitHub Pages (set automatically in CI). For local static export testing:
# NEXT_PUBLIC_BASE_PATH=/DanielSaar
# NEXT_PUBLIC_SITE_URL=https://maor-ar.github.io/DanielSaar

# After you buy a custom domain, set in GitHub repo → Settings → Secrets and Variables → Actions:
# NEXT_PUBLIC_SITE_URL=https://your-domain.com
# NEXT_PUBLIC_BASE_PATH=   (empty when using a custom domain at the root)
